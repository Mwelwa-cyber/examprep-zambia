/**
 * @deprecated — superseded by PlayGame.jsx (Firestore-driven flow).
 * Left as a stub because the workspace mount blocks file deletion.
 * No routes import this file anymore.
 */
export default function GameRouter() {
  return null
}
