/**
 * @deprecated — superseded by GamesShell.jsx (consistent chrome across
 * every /games page). Left as a stub because the workspace mount blocks
 * file deletion. No code imports this file anymore.
 */
export default function GameLayout() {
  return null
}
