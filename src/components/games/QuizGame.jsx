/**
 * @deprecated — superseded by TimedQuizGame.jsx (generic `timed_quiz`
 * engine that reads from the Firestore `games` collection).
 * Left as a stub because the workspace mount blocks file deletion.
 */
export default function QuizGame() {
  return null
}
