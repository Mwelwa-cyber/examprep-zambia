/**
 * @deprecated — memory_match engine will be rebuilt to read from the
 * Firestore `games` collection (type: "memory_match"). See PlayGame.jsx
 * for the new dispatch pattern. Left as a stub because the workspace
 * mount blocks file deletion.
 */
export default function MemoryGame() {
  return null
}
