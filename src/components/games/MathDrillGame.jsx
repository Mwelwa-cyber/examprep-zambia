/**
 * @deprecated — replaced by TimedQuizGame.jsx which handles any
 * `type: "timed_quiz"` document. Left as a stub because the workspace
 * mount blocks file deletion.
 */
export default function MathDrillGame() {
  return null
}
