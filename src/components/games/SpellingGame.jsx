/**
 * @deprecated — spelling engine will be rebuilt to read from the
 * Firestore `games` collection. Left as a stub because the workspace
 * mount blocks file deletion.
 */
export default function SpellingGame() {
  return null
}
