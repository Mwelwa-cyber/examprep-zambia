/**
 * @deprecated — superseded by:
 *   - src/data/gamesSeed.js  (curated seed in proper Firestore shape)
 *   - src/utils/gamesService.js  (Firestore client)
 *
 * Left as a stub because the workspace mount blocks file deletion.
 * No code imports this file anymore.
 */
export const QUIZ_SETS = []
export const MEMORY_SETS = []
export const SPELLING_SETS = []
export const MATH_SETS = []
export const GAME_TYPES = []
export function findSet() { return null }
export function buildMathRound() { return [] }
